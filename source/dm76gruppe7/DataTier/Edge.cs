using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DataTier
{
    public class Edge
    {
        /*public int _id { get; private set; }
        public int _distance { get; private set; }
        public Node _startNode { get; private set; }
        public Node _endNode { get; private set; }

        public Edge()
        {
            _id = -1;
            _distance = -1;
            _startNode = null;
            _endNode = null;
        }

        public Edge(int distance, Node startNode, Node endNode)
        {
            _id = -1;
            _distance = distance;
            _startNode = startNode;
            _endNode = endNode;
        }

        public Edge(int id, int distance, Node startNode, Node endNode)
        {
            _id = id;
            _distance = distance;
            _startNode = startNode;
            _endNode = endNode;
        }*/
    }
}
